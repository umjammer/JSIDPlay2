Release Notes:
==============

JSIDPlay2:
Default Java version has been changed to Java11 and higher, however java8 can still be used. There are separate executables and shell scripts where java8 is part of it's name.
Bugfix: Changing sub-tunes was impossible, if favorites playback option was turned off.
Sub song ordering can be configured in the Player menu to start with 1 instead of the start song. Default is true. (E.G. Last_Ninja.sid with start song 3: 1,2,(3)..11) instead of (3)..11,1,2 for the UI version.
Song recording are delayed for RSID about 2.5 seconds (time to RESET) leading in too short songs e.g. in recordings, this has been fixed
ConsolePlayer can now play or record whole folders. This is to support batch recording.
ConsolePlayer's verbose mode now shows the filters being used
ConsolePlayer validates and reports wrong filter names, now

JSIDPlay2Server (https://haendel.ddns.net:8443/static/c64jukebox.vue):
- Use of HTTP2 protocol and compression
- Use of UTF8 filenames
- Support of hls.js or video.js as video driver)
- Load disk directory entries
- Hardware USB drivers have been added to make use of USB devices in Chrome Browser, Microsoft Edge or Opera.
  This works for PC and Mobile devices.
- Service added to fetch Directory contents
- Service added to fetch SID tune information list
