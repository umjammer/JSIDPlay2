Release Notes:
==============

JSIDPlay2:
- All shell-scripts and desktop files renamed to lower case without space in it's name better for convenience
- splash screen removed
- New java based USB drivers implemented for:
  SIDBlaster, ExSID, ExSID+, HardSID4U, HardSID Uno and HardSID UPlay
  Supported platforms: Windows/Linux and MacOSX (MacOSX currently not working with HardSID devices)
- SID Recordings can now being watched and exported in a new recording tab.
- Player control buttons re-arranged

JSIDPlay2Server:
- Live videos: can be controlled now by web-page (insert next disk, send keys and so on)
- Live videos: shows info now in a status bar
- Live videos: bar-code to watch videos on your mobile phone

JSIDPlay2app:
- SIDBlaster, ExSID, ExSID+, HardSID4U, HardSID Uno and HardSID UPlay can now be connected and used standalone on a mobile device.
